# Python dependencies for running the SMART experiments end-to-end.
# The `smart` package itself is installed separately with `pip install -e ./smart`.

# Core scientific stack (also pulled in as smart dependencies).
numpy>=1.23
scipy>=1.9
scikit-learn>=1.1
matplotlib>=3.5
joblib>=1.2
optuna>=3.0
autograd>=1.5
pymanopt>=2.1

# Used only by the single-cell pipeline.
scanpy>=1.9
anndata>=0.9
prettytable>=3.0

# Test runner.
pytest>=7.0
